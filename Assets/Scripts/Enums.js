#pragma strict

enum GameMode {
	ScoreTheirs,
	ScoreYours
}

enum PowerUpModes {
	None = 0,
	IncreaseMass = 1,
	DecreaseMass = 2
	// Additional modes
	// Shift fulcrum left/right
	// increase/disable jump?
	// increase/decrease run speed
}
